{
  "name": "investment-news-collector",
  "version": "1.0.0",
  "description": "Investment news collector for Pathway Group",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "netlify dev",
    "build": "netlify build",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "axios": "^1.6.7"
  }
}